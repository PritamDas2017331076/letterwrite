import React from 'react'

const Recordlayout = ({dataid,userid,name,data}) => {
    return (
        <div className="item">
            <div>{name}</div>
        </div>
    )
}

export default Recordlayout
